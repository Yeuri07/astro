---
import BaseLayout from "../layouts/BaseLayout.astro"
const pageTitle = "Sobre mi";

const identy = {
  name: 'yeuri',
  lastname: 'Reyes',
  age: 25,
  country: 'Republica Dominicana',
  city: 'Santo Domingo',
  ocupation: 'fullstack developer',
  hobies: ['programar', 'leer', 'jugar videojuegos', 'ver peliculas'],

};

const skill = ["HTML", "CSS", "JavaScript", "React", "Node.js", "MongoDB", "Git", "GitHub", "Astro"]

const happy = true;
const finished = false;
const goal = 3;

// const skillColor = "navy"
// const fontWeight = "bold"
// const textCase = "uppercase"

---



<BaseLayout pageTitle = {pageTitle} text = 'h1-about' >


  <!-- <style define:vars={{skillColor, fontWeight, textCase}}>
    h1 {
      color: purple;
      font-size: 4rem;
     
    }
    .skill {
      color: var(--skillColor);
      font-weight: var(--fontWeight);
      text-transform: var(--textCase);
      }
  </style> -->


<h2>... ¡y mi nuevo sitio Astro!</h2>
  
    <p>Estoy trabajando en el tutorial introductorio de Astro. Esta es la segunda página de mi sitio web, ¡y es la primera que he construido yo mismo!</p>
  
    <p>Este sitio se irá actualizando a medida que vaya completando más partes del tutorial, ¡así que no dejes de visitarlo para ver cómo va mi viaje!</p>
    
    <ul>
      <li> Me llamo {identy.name}</li>
      <li>Vivo en {identy.lastname} y trabajo como {identy.ocupation}.</li>
      {identy.hobies.length >=  2 &&
      <ul> Mis aficiones son {identy.hobies.map((hobie) => <li>{hobie}</li>)}</ul>
      }
    </ul>
      <p>Mis habilidades son:</p>

      <ul>
        {skill.map((skill) => <li class="skill">{skill}</li>)}
      </ul>

      {happy && <p>Estoy feliz de aprender Atro!</p>}
      {finished && <p>He terminado el tutorial de Astro!</p>}
      {goal === 3 ? <p>Mi objetivo es terminar en 3 dias.</p> : <p>Mi objectivo no son 3 dias.</p>}

</BaseLayout>




 
 
 